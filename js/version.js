/* =====================================================================
   GROCERY STORE SIMULATOR — the version number
   =====================================================================

   One string, in one place, printed small in the corner of the title
   screen so a report of "it does this" can say WHICH it. Bump it here
   and nowhere else: the page reads it at start-up, the smoke test
   checks the page does, and nothing else in the game cares.

   MAJOR.MINOR.PATCH, the usual way round: the middle number for a
   change a player would notice (a weapon, a level, a control scheme),
   the last for a fix, the first for the day it is finished. */
export const VERSION = '0.2.0';
